interface:
  display_name: "Auto Dispatch"
  short_description: "Route tasks to the right Codex model"
  default_prompt: "Use $auto-dispatch to rate this task with GPT-5.6 Sol Max, continue it in a new task using the selected model and reasoning level, then archive this task."
policy:
  allow_implicit_invocation: false
