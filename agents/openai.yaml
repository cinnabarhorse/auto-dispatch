interface:
  display_name: "Auto Dispatch"
  short_description: "Route tasks and eligible Goals to the right model"
  default_prompt: "Use $auto-dispatch to rate this task with GPT-5.6 Sol Max, continue it in a new task using the selected model and reasoning level, transfer an eligible active Goal, then archive this task."
policy:
  allow_implicit_invocation: false
