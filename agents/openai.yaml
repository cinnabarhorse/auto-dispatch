interface:
  display_name: "Auto Dispatch"
  short_description: "Route eligible tasks with interruption-safe reservations"
  default_prompt: "Use $auto-dispatch to rate this task once with GPT-5.6 Sol Max, reserve at most one destination task using the chosen model and reasoning level, and always leave this task active."
policy:
  allow_implicit_invocation: false
